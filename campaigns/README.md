# campaigns

This folder holds file-backed campaign instances.

Current checked-in example:
- `2026-03-tw-vcp-shadow-candidate/` — initialized from `examples/steamer-vcp-mandate.yaml` via `tools/init-campaign.py`

Purpose of the checked-in example:
- prove that `validate-mandate.py` + `init-campaign.py` work end-to-end
- provide a small reference campaign directory for `advance-campaign.py` / `inspect-campaign.py`
